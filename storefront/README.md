# Storefront

The active H&M recommendation storefront. It uses vinext/React, ships as a Cloudflare-compatible worker, and consumes the typed catalog plus baseline recommender in `app/data/catalog.ts`.

```bash
npm install
npm run dev
npm run build
```

The root project README documents the recommendation logic and TIGER roadmap.
